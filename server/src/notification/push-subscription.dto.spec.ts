import { PushSubscriptionDto } from './push-subscription.dto';

describe('PushSubscriptionDto', () => {
  it('should be defined', () => {
    expect(new PushSubscriptionDto()).toBeDefined();
  });
});
